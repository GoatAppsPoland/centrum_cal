# Centrum klienta cal.pl 

## Install TailwindCSS
`npm install -D tailwindcss`  
`npx tailwindcss init`
## Start the Tailwind CLI build process
`npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch`
